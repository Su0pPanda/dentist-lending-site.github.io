import type { ClinicContent } from '../../domain/clinic'
import { buildWhatsAppUrl } from '../../lib/whatsapp'
import { Section } from '../layout/Section'
import { ButtonLink } from '../ui/ButtonLink'
import { PlaceholderBadge } from '../ui/PlaceholderBadge'
export function ContactsSection({ content }: { content: ClinicContent }) { const href = buildWhatsAppUrl(content.clinic.whatsappNumber.value, { type: 'generic', source: 'Раздел контактов' }); return <Section id="contacts" className="contacts-section" eyebrow="Контакты" title="Будем рады познакомиться" intro="Напишите нам или приезжайте на консультацию — подскажем, с чего начать."><div className="contacts-grid"><div className="contact-list"><div><span>Адрес</span><strong>{content.clinic.address.value}</strong></div><div><span>Телефон</span><strong>{content.clinic.phoneDisplay.value}</strong></div><div><span>График работы</span>{content.clinic.workingHours.map((line) => <strong key={line.value}>{line.value}</strong>)}</div><ButtonLink href={href}>Написать в WhatsApp</ButtonLink><PlaceholderBadge visible={content.clinic.address.placeholder}/></div><div className="map-card"><img src={content.clinic.map.src} alt={content.clinic.map.alt} width={content.clinic.map.width} height={content.clinic.map.height} loading="lazy"/><div className="map-pin" aria-hidden="true">⌖</div><PlaceholderBadge visible={content.clinic.map.placeholder}/></div></div></Section> }
