import type { ClinicContent } from '../../domain/clinic'
import { Section } from '../layout/Section'
import { Card } from '../ui/Card'
import { PlaceholderBadge } from '../ui/PlaceholderBadge'
export function CasesReviewsSection({ content }: { content: ClinicContent }) { return <Section id="results" className="results-section" eyebrow="Результаты и отзывы" title="Улыбки, за которыми стоят реальные истории" intro="Показываем путь лечения честно и бережно. Фотографии публикуются только с согласия пациента."><div className="case-grid">{content.cases.map((item) => <Card key={item.id} className="case-card"><div className="before-after"><figure><img src={item.beforeImage.src} alt={item.beforeImage.alt} width={item.beforeImage.width} height={item.beforeImage.height} loading="lazy"/><figcaption>До</figcaption></figure><figure><img src={item.afterImage.src} alt={item.afterImage.alt} width={item.afterImage.width} height={item.afterImage.height} loading="lazy"/><figcaption>После</figcaption></figure></div><h3>{item.summary.value}</h3><p className="disclaimer">{item.disclaimer}</p><PlaceholderBadge visible={item.placeholder}/></Card>)}</div><div className="reviews-grid">{content.reviews.map((review) => <Card key={review.id} className="review-card"><div className="stars" aria-label={`${review.rating} из 5 звёзд`}>{'★'.repeat(review.rating)}</div><blockquote>«{review.text.value}»</blockquote><p>{review.author.value}</p><PlaceholderBadge visible={review.placeholder}/></Card>)}</div></Section> }
